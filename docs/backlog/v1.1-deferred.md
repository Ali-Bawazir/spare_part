# MMS v1.1 — Deferred Backlog

Items identified during v1.0.0 hardening that do NOT block production. Captured here for v1.1 planning.

## Performance

- N+1 in `work_order_detail` (4 separate related-records queries). Add `select_related`/`prefetch_related` + consolidate.
- `SparePart.image_status` / `image_url` properties issue one query per part in stock dashboard. Annotate via `OuterRef`/`Subquery`.
- `legacy_count` query in `context_processors.py` runs on every nav render. Cache via signal-driven recompute.
- `_free_subq` double-nested subquery in work order detail free-stock annotation.
- Long views.py files (>2000 lines) split into smaller modules.

## Code quality

- `qty_str` dead branch in `maintenance/notifications.py:610`.
- `import logging` placed mid-file in `procurement/services.py:91-93`.
- Placeholder `inventory/tests.py` and `procurement/tests.py` (one-line stubs).
- `procurement/services.py:91-93` `import logging` after function defs.

## Logging & observability

- Structured JSON logging (current: human-readable stderr).
- `/health/` Redis check (Redis is Phase 2 per CONTEXT.md).
- Custom 404/500 friendly error pages.
- Custom CSRF failure view.
- AuditEntry IP/User-Agent capture (currently only actor + payload).
- AuditEntry tamper-evidence (currently deletable by super-admin).

## Files & attachments

- Pillow `Image.verify()` magic-byte validation is partially implemented (images + PDF). Extend to audio/video when those uploads grow.
- `python-magic` for general MIME sniffing (currently rely on Pillow for images + 4-byte check for PDF).
- Manager can edit/delete attachments (currently only owner + super-admin).

## Documentation

- `SECURITY.md` (vulnerability reporting process).
- `CHANGELOG.md` (Keep a Changelog format).
- `OPERATIONS.md` (admin tasks per role, beyond RUNBOOK).
- Noto Sans Arabic TTF bundled in `static/fonts/arabic/` so PDFs render Arabic text instead of boxes.
- check_i18n 16 known issues resolved.

## Test coverage

- 247/247 test target on PostgreSQL (currently ~228/251 with pre-existing failures).
- Real-image QR stress tests (currently mocked `_decode_uploaded_qr`).
- `pip-compile` requirements pinning for reproducible builds.
- E2E test for authentication flow (login, logout, inactive user).

## Configuration

- Move to `pip-compile` for pinned requirements.
- Treebeard / django-mptt for asset hierarchy (current Python recursion acceptable at ≤5 levels).
- Celery + Redis + cron (currently deferred to Phase 2 — manual cron jobs run instead).

## Notifications

- `Notification.dedup_key` (currently uses fragile `body__contains`).
- `pm_overdue_notifications` body-tag dedup index.
- `_send_email_if_configured` is a no-op stub (documented Phase 2).

## Known pre-existing test failures

These are NOT regressions from v1.0.0 hardening; they exist in the in-flight working tree and are waived for v1.0.0. Tracked here so v1.1 can address them:

- `inventory.test_chokepoint_reservations.EditShortageDecisionTopUpTests.test_edit_shortage_decision_top_up_via_allocate_one`
- `inventory.test_execute_warehouse_issue_repair_mode.*` (8 tests)
- `inventory.test_phase78_reservation_lifecycle.{CancelApprovedLineReservationTests, ReservationAggregateSyncTests}`
- `procurement.test_supplier_backfill.*` (2 tests)
- `procurement.test_supplier_intelligence.*` (10 tests)
- `procurement.test_po_receive_v2.PurchaseOrderReceiveV2Tests.test_receive_calls_reallocate_for_part` (auto-fulfill changed flow)
- `maintenance.test_blocker_system.{PartBlockerHookTests.test_warehouse_issue_does_not_resolve_blocker_when_partial, WorkOrderBlockerServiceTests.test_sync_from_external_event_part_issued_resolves_blocker}`
- `maintenance.test_phase77_auto_fulfill.{AutoFulfillmentTests.test_auto_fulfill_distributes_to_multiple_lines, AutoFulfillmentTests.test_auto_fulfill_partial_qty_issues_received}`